{
  "name": "cross-border-customer-service-api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/test/*.test.js",
    "typecheck": "tsc --noEmit",
    "start": "node dist/src/main.js"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
